package com.ausaf.sudoku.entity;

/** Final result of a completed {@link MultiplayerGame}: which player won, or that it drew. */
public enum MultiplayerGameOutcome {
    PLAYER1_WIN,
    PLAYER2_WIN,
    DRAW
}
